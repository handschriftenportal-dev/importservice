export default class JobDetails {

  constructor(id, name, typ, format, valide, ergebnis, fehler) {
    this.id = id;
    this.name = name;
    this.typ = typ;
    this.format = format;
    this.valide = valide;
    this.ergebnis = ergebnis;
    this.fehler = fehler;
  }

}
